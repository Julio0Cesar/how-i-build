import type { Project } from "./types";

/**
 * Empty until the first real case study lands (#19). The template's sample
 * files stay in `src/content/projects/` — deleting files `main` owns would
 * turn every future template change into a delete/modify conflict on the sync.
 */
export const projects: Project[] = [];
