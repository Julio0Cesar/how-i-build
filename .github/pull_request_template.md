<!-- What changed and why. The diff already says what; use this for the why. -->

Closes #

## Verified

<!--
Run with `.next/` removed, and check exit codes rather than output — the build
prints its route table before it can still fail.

    bun install && bun run typecheck && bun run lint && bun run build
-->

- [ ] `typecheck`
- [ ] `lint`
- [ ] `build`
